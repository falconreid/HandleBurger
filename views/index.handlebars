<!-- main section -->
<header class="container">
    <div class="row">
        <div class="col">

        </div>
        <div class="col-6">
            <img src="../img/handle-burger-bar.png" alt="Handle Burger Bar logo" title="Handle Burger Bar">
        </div>
        <div class="col">

        </div>
    </div>
</header>

<main id="form" class="container">
    <div class="row">
        <div class="col">

        </div>
        <div class="col-4">
            <h2>Add a Burger</h2>
            <form id="createBurger" class="button-size">
                <textarea type="text" name="burger"></textarea>
                <button type="submit">Save Burger</button>
            </form>
        </div>
        <div class="col">

        </div>
    </div>
</main>
<hr>

<div id="split" class="container">
    <div class="row">
        <div id="Eat" class="col">
            <h3>Burgers to be Eaten</h3>

            <ul>
                {{#each burger}}
                <li>
                    <p>
                        {{this.id}}. {{this.burger}}

                        <button data-burgerid="{{this.id}}" class="eatBurger">Devour the Burger</button>
                    </p>
                </li>
                {{/each}}
            </ul>
        </div>
        <div class="col">
            <h3>Burgers I've Eaten</h3>
            <ul>
                {{#each eatenBurger}}
                <li>
                    <p>
                        {{this.id}}. {{this.eatenBurger}}

                        <button data-burgerid="{{this.id}}" class="delBurger">Clear</button>
                    </p>
                </li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>